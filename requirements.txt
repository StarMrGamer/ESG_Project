openai>=1.0
requests>=2.28
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
httpx>=0.27  # FastAPI TestClient used by the offline API smoke tests

# OPTIONAL — only for writing/reading the Sepolia evidence anchor (Build Spec C3). The engine,
# the harness and the whole demo run fine without it: an unreachable chain degrades to
# `anchor_pending`, which the UI shows rather than hiding.
# web3>=6.0

# OPTIONAL — only for `scripts/build_cost_workbook.py`, which writes the business team's cost
# model as a real calculating .xlsx. Nothing in the app, the engine or the tests imports it;
# `cost_model.py` computes the same figures without it and cross-checks the workbook's output.
# openpyxl>=3.1
